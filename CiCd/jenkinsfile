pipeline{
    agent {
        docker { image: 'mcr.microsoft.com/dotnet/core/sdk'}
    }
    stages{
        stage('checkout') {
            steps {
                checkout scm
            }
        }
        stage("build"){
            steps{
                echo "========executing Build========"
            }
        }
    }    
}